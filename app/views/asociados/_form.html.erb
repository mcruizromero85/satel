<%= form_for(@asociado) do |f| %>
  <% if @asociado.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@asociado.errors.count, "error") %> prohibited this asociado from being saved:</h2>

      <ul>
      <% @asociado.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :descripcion %><br>
    <%= f.text_field :descripcion %>
  </div>
  <div class="field">
    <%= f.label :juego_id %><br>
    <%= f.number_field :juego_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
